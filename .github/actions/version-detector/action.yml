name: "Version Detector"
description: "Determines version bump type based on conventional commits"
author: "MIEW"
inputs: {}
outputs:
  bump_type:
    description: "Determined bump type (major, minor, patch, none)"
runs:
  using: "node24"
  main: "index.js"
